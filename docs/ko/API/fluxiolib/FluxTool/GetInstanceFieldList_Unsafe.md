# FluxTool.GetInstanceFieldList_Unsafe method (1 of 2)

모든 필드를 조회하여 필드 설명자 배열을 얻습니다. 정적 필드도 함께 검색됩니다.

```csharp
public static FluxRuntimeFieldDesc[] GetInstanceFieldList_Unsafe(Type t)
```

| parameter | description |
| --- | --- |
| t | 정보를 읽을 타입입니다. |

## Return Value

조회된 모든 필드의 필드 설명자가 들어있는 배열입니다.

## Remarks

이 API는 매개변수나 개체 상태의 유효성을 검사하지 않습니다.

## See Also

* struct [FluxRuntimeFieldDesc](../FluxRuntimeFieldDesc.md)
* class [FluxTool](../FluxTool.md)
* namespace [fluxiolib](../../fluxiolib.md)

---

# FluxTool.GetInstanceFieldList_Unsafe method (2 of 2)

주어진 조건에 따라 필터링하여 지정된 형식의 모든 필드를 조회합니다.

```csharp
public static IEnumerable<FluxRuntimeFieldDesc> GetInstanceFieldList_Unsafe(Type t, 
    ProtFlags protection = ProtFlags.Public, TypeFlags cortype = TypeFlags.ALL, 
    FluxSearchSpace space = default)
```

| parameter | description |
| --- | --- |
| protection | 찾고자하는 필드의 접근제한자 특징 마스크입니다. 이 값은 보통 우선 순위로 탐색에 사용됩니다. |
| cortype | 찾고자하는 필드의 타입 특징 마스크입니다. 이 값은 보통 우선 순위로 탐색에 사용됩니다. |
| space | 찾고자하는 필드의 검색 공간 범위 입니다. 이 값은 높은 순위로 탐색에 사용됩니다. |
| t | 검색을 수행할 대상 타입을 표현하는 객체입니다. |

## Return Value

주어진 조건으로 필터링을 수행하는 열거자 객체의 참조입니다. 조건에 맞는 필드가 없거나, 메소드가 실패하면 빈 배열에 대한 열거자 객체의 참조입니다.

## Remarks

이 API는 매개변수나 개체 상태의 유효성을 검사하지 않습니다.

## See Also

* struct [FluxRuntimeFieldDesc](../FluxRuntimeFieldDesc.md)
* enum [ProtFlags](../ProtFlags.md)
* enum [TypeFlags](../TypeFlags.md)
* struct [FluxSearchSpace](../FluxSearchSpace.md)
* class [FluxTool](../FluxTool.md)
* namespace [fluxiolib](../../fluxiolib.md)

<!-- DO NOT EDIT: generated by xmldocmd for fluxiolib.dll -->
