# FluxTool.GetInstanceField method (1 of 2)

인스턴스 필드를 검색합니다.

```csharp
public static FluxRuntimeFieldDesc GetInstanceField(Type t, SStringUtf8 utf8Name, 
    ProtFlags protection = ProtFlags.Public, TypeFlags cortype = TypeFlags.ALL, 
    FluxSearchSpace space = default)
```

| parameter | description |
| --- | --- |
| utf8Name | UTF8로 인코딩된 이름으로, 찾을 이름입니다. 이 값은 낮은 우선 순위로 탐색에 사용됩니다. |
| protection | 찾고자하는 필드의 접근제한자 특징 마스크입니다. 이 값은 보통 우선 순위로 탐색에 사용됩니다. |
| cortype | 찾고자하는 필드의 타입 특징 마스크입니다. 이 값은 보통 우선 순위로 탐색에 사용됩니다. |
| space | 찾고자하는 필드의 검색 공간 범위 입니다. 이 값은 높은 순위로 탐색에 사용됩니다. |
| t | 검색을 수행할 대상 타입을 표현하는 객체입니다. |

## Return Value

메소드가 성공하여 필드를 찾은 경우 [`IsNull`](../FluxRuntimeFieldDesc/IsNull.md) = `false` 입니다. 그렇지 않은 경우, `true` 입니다.

## Remarks

*t*가 IsGenericTypeDefinition를 True로 표시하면 이 메소드는 항상 실패합니다.

## See Also

* struct [FluxRuntimeFieldDesc](../FluxRuntimeFieldDesc.md)
* enum [ProtFlags](../ProtFlags.md)
* enum [TypeFlags](../TypeFlags.md)
* struct [FluxSearchSpace](../FluxSearchSpace.md)
* class [FluxTool](../FluxTool.md)
* namespace [fluxiolib](../../fluxiolib.md)

---

# FluxTool.GetInstanceField&lt;T&gt; method (2 of 2)

인스턴스 필드를 검색합니다.

```csharp
public static FluxRuntimeFieldDesc GetInstanceField<T>(SStringUtf8 utf8Name, 
    ProtFlags protection = ProtFlags.Public, TypeFlags cortype = TypeFlags.ALL, 
    FluxSearchSpace space = default)
```

| parameter | description |
| --- | --- |
| T | 검색을 수행할 대상 유형(Type)입니다. |
| utf8Name | UTF8로 인코딩된 이름으로, 찾을 이름입니다. 이 값은 낮은 우선 순위로 탐색에 사용됩니다. |
| protection | 찾고자하는 필드의 접근제한자 특징 마스크입니다. 이 값은 보통 우선 순위로 탐색에 사용됩니다. |
| cortype | 찾고자하는 필드의 타입 특징 마스크입니다. 이 값은 보통 우선 순위로 탐색에 사용됩니다. |
| space | 찾고자하는 필드의 검색 공간 범위 입니다. 이 값은 높은 순위로 탐색에 사용됩니다. |

## Return Value

메소드가 성공하여 필드를 찾은 경우 [`IsNull`](../FluxRuntimeFieldDesc/IsNull.md) = `false` 입니다. 그렇지 않은 경우, `true` 입니다.

## See Also

* struct [FluxRuntimeFieldDesc](../FluxRuntimeFieldDesc.md)
* enum [ProtFlags](../ProtFlags.md)
* enum [TypeFlags](../TypeFlags.md)
* struct [FluxSearchSpace](../FluxSearchSpace.md)
* class [FluxTool](../FluxTool.md)
* namespace [fluxiolib](../../fluxiolib.md)

<!-- DO NOT EDIT: generated by xmldocmd for fluxiolib.dll -->
